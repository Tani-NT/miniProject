<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>miniProject</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen font-[gilroy] bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a href="/logout" class="bg-red-500 rounded-md px-4 py-2">Logout</a>
        </div>
        <h3 class="text-[2vw]">hello, <%= user.name %></h3>
        <h5 class="mb-5">You can create your post here..</h5>
        <form action="/post" method="post">
            <textarea name="content" placeholder="What's on your mind..?" class="w-1/2 block resize-none p-3 bg-transparent outline-none border-2 border-zinc-800 rounded-md"></textarea>
            <input class="px-8 cursor-pointer font-semibold py-2 bg-blue-500 rounded-md block mt-4" type="submit" value="Post">
        </form>
        <div class="posts mt-12">
            <h3 class="text-zinc-400">Your posts</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/2 p-3 mb-4 border-[1px] rounded-md bg-zinc-800 border-zinc-700">
                        <h4 class="text-[1.8vw] text-blue-500 font-semibold mb-1">@<%= user.username %></h4>
                        <p class="text-[1.3vw]"><%= post.content %></p>
                        <div class="btns flex gap-6 mt-3">
                            <a class="text-blue-500" href="">Like</a>
                            <a class="text-zinc-500" href="">edit</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>